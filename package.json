{
  "name": "react-messenger-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack messenger application with React client and Node.js server.",
  "scripts": {
    "install": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "",
  "license": "ISC"
}